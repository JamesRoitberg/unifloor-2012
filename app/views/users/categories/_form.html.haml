= form_for [:users, @users_category] do |f|
  -if @users_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@users_category.errors.count, "error")} prohibited this users_category from being saved:"
      %ul
        - @users_category.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Salvar'


